name: flod
description: Strict, fast, type-safe data validation and transformation for Dart and Flutter.
version: 1.1.2
repository: https://github.com/leeerrrrmmm/Flod-Library/tree/main/flod

topics:
  - validation
  - forms
  - json
  - dio
  - schema

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.12.0
  dio: ^5.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

